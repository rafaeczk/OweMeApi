Rest api written in c# asp.net core, organized following domain-driven-design approach with cqrs (MediatR), using entity-framework.
Signed in user can create debts with their friends, verify payments and approve debts. Every operation is stored in the database with its timestamp, so it makes easy to review the debt history.
